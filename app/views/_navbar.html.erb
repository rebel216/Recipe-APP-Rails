<header class="" >
  <div class="header">
    <div>
      <h3 class="">Rails Recipe APP</h3>
    </div>
    <div>
      <nav class="nav">
        <ul class="nav-wrapper">
          <li><%= link_to "Public recipes", public_recipes_path%>
          </ul>
        </nav>
      </div>
      <div>
        <% if user_signed_in? %>
          <%= link_to"Food", user_foods_path(current_user.id), method: :get%>
          
          <%= link_to "Recipes", recipes_path %>
          <%= link_to "Food", user_foods_path(current_user) %>
          <%= link_to "Shoppings", user_shoppings_path(current_user) %>
          <%= link_to "Sign out", destroy_user_session_path, method: :delete%>

        <% else %>

          <%= link_to "Log in", new_user_session_path %>
          <%= link_to "Sign up", new_user_registration_path %>
        <% end %>
      </div>
    </div>
  </header>
